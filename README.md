# Flow Game
Tiny project showcasing how to utilize [Flow Graph](https://github.com/MothCocoon/FlowGraph) plugin. 

### **Note:** This project requires `Git LFS` to work properly; `zip` downloads will not work. It's recommended to grab the project by cloning the repository.
